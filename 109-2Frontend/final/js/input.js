imgInp1.onchange = evt => {
  const [file] = imgInp1.files;
  if (file) {
    img1.src = URL.createObjectURL(file);
    card11.src= URL.createObjectURL(file);
    card12.src= URL.createObjectURL(file);
  }
}

imgInp2.onchange = evt => {
  const [file] = imgInp2.files;
  if (file) {
    img2.src = URL.createObjectURL(file);
    card21.src= URL.createObjectURL(file);
    card22.src= URL.createObjectURL(file);
  }
}

imgInp3.onchange = evt => {
  const [file] = imgInp3.files;
  if (file) {
    img3.src = URL.createObjectURL(file);
    card31.src= URL.createObjectURL(file);
    card32.src= URL.createObjectURL(file);
  }
}

imgInp4.onchange = evt => {
  const [file] = imgInp4.files;
  if (file) {
    img4.src = URL.createObjectURL(file);
    card41.src= URL.createObjectURL(file);
    card42.src= URL.createObjectURL(file);
  }
}

imgInp5.onchange = evt => {
  const [file] = imgInp5.files;
  if (file) {
    img5.src = URL.createObjectURL(file);
    card51.src= URL.createObjectURL(file);
    card52.src= URL.createObjectURL(file);
  }
}

imgInp6.onchange = evt => {
  const [file] = imgInp6.files;
  if (file) {
    img6.src = URL.createObjectURL(file);
    card61.src= URL.createObjectURL(file);
    card62.src= URL.createObjectURL(file);
  }
}

imgInp7.onchange = evt => {
  const [file] = imgInp7.files;
  if (file) {
    img7.src = URL.createObjectURL(file);
    card71.src= URL.createObjectURL(file);
    card72.src= URL.createObjectURL(file);
  }
}

imgInp8.onchange = evt => {
  const [file] = imgInp8.files;
  if (file) {
    img8.src = URL.createObjectURL(file);
    card81.src= URL.createObjectURL(file);
    card82.src= URL.createObjectURL(file);
  }
}

imgInp9.onchange = evt => {
  const [file] = imgInp9.files;
  if (file) {
    img9.src = URL.createObjectURL(file);
    card91.src= URL.createObjectURL(file);
    card92.src= URL.createObjectURL(file);
  }
}

imgInp10.onchange = evt => {
  const [file] = imgInp10.files;
  if (file) {
    img10.src = URL.createObjectURL(file);
    card101.src= URL.createObjectURL(file);
    card102.src= URL.createObjectURL(file);
  }
}

imgInp11.onchange = evt => {
  const [file] = imgInp11.files;
  if (file) {
    img11.src = URL.createObjectURL(file);
    card111.src= URL.createObjectURL(file);
    card112.src= URL.createObjectURL(file);
  }
}

imgInp12.onchange = evt => {
  const [file] = imgInp12.files;
  if (file) {
    img12.src = URL.createObjectURL(file);
    card121.src= URL.createObjectURL(file);
    card122.src= URL.createObjectURL(file);
  }
}

imgInp13.onchange = evt => {
  const [file] = imgInp13.files;
  if (file) {
    img13.src = URL.createObjectURL(file);
    card131.src= URL.createObjectURL(file);
    card132.src= URL.createObjectURL(file);
  }
}

imgInp14.onchange = evt => {
  const [file] = imgInp14.files;
  if (file) {
    img14.src = URL.createObjectURL(file);
    card141.src= URL.createObjectURL(file);
    card142.src= URL.createObjectURL(file);
  }
}

imgInp15.onchange = evt => {
  const [file] = imgInp15.files;
  if (file) {
    img15.src = URL.createObjectURL(file);
    card151.src= URL.createObjectURL(file);
    card152.src= URL.createObjectURL(file);
  }
}